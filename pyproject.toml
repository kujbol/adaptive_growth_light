[project]
name = "adaptive_growth_light"
version = "1.0.0"
description = "Adaptive supplementary plant lighting integration for Home Assistant"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "astral>=3.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
